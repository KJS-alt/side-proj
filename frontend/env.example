# Vite 환경 변수 예시: 시연 모드에서는 백엔드와 같은 호스트를 사용하므로 상대경로(/api)를 기본값으로 둡니다.
VITE_API_BASE_URL=/api

# 개발 중 프론트엔드를 별도 포트(예: 5173)에서 돌리면서 API 서버를 8081로 이용하고 싶다면 아래 설정을 사용하세요.
# VITE_API_BASE_URL=http://localhost:8081/api

